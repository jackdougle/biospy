[package]
name = "biospy-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
ahash = "0.8"

[dev-dependencies]
tempfile = "3"
